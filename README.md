download python 3.10 
create a virtual envirnoment on python 3.10 
install library mediapipe==0.8.9.1
numpy==1.21.6
opencv-python==4.5.5.64
pandas==1.3.5
then install pip uninstall protobuf -y
pip install protobuf==3.20.3
since protobuf = 3.20.* is safer version to run mediapipe 
copy all file in the virtual envirnoment folder
Then run the main.py to run code in ternimal 
give path of video file 

